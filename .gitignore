# deps
node_modules/
.pnp.*

# env
.env
.env.*
!.env.example

# foundry
contracts/out/
contracts/cache/
contracts/broadcast/

# ponder
indexer/.ponder/
indexer/generated/

# vite
web/dist/
web/.vite/

# docs copied into the app at build by scripts/sync-docs.mjs (source of truth is /docs)
web/public/docs/

# engagement server (sqlite + uploaded media blobs)
server/*.sqlite
server/*.sqlite-shm
server/*.sqlite-wal
server/media/

# logs / os
*.log
.DS_Store
.tmp/

# local agent config / skills (framer-council is a symlink into another repo)
.claude/
